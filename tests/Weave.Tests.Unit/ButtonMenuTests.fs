module Weave.Tests.Unit.ButtonMenuTests

open Expecto
open Weave
open Weave.CssHelpers

[<Tests>]
let buttonMenuTests =
  testList "ButtonMenu" [
  // Direction uses direct let-binding attrs (top/bottom/left/right).
  // TypedCssClasses enforces correctness at compile time — no runtime tests needed.
  ]
